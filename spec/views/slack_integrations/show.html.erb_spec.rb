require 'rails_helper'

RSpec.describe "slack_integrations/show", type: :view do
  # before(:each) do
  #   @slack_integration = assign(:slack_integration, SlackIntegration.create!(
  #     :channel => "Channel",
  #     :webhook_url => "MyText"
  #   ))
  # end

  # it "renders attributes in <p>" do
  #   render
  #   expect(rendered).to match(/Channel/)
  #   expect(rendered).to match(/MyText/)
  # end
end
